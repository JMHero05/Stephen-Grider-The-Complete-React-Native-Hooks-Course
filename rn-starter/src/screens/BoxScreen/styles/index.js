import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  viewStyle: {
    borderWidth: 3,
    borderColor: 'black',
    flexDirection: 'row',
    height: 200,
  },
  textStyle: {
    borderWidth: 3,
    borderColor: 'red',
  },
});
